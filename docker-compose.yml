services:
  danish-intelligence:
    build: .
    image: danish-intelligence:latest
    container_name: danish-intelligence
    restart: unless-stopped
    env_file: [master.env]
    volumes:
      - /srv/config/danish-intelligence:/config
    networks:
      - media-stack

networks:
  media-stack:
    external: true
